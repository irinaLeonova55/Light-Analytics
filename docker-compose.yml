version: "3.5"
services:
  light_promo:
    container_name: light_promo
    image: light-landing-promo
    restart: always
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3001:3000" # Пробрасываем порт 3001 хоста на порт 3000 контейнера
    # env_file:
    #  - .env